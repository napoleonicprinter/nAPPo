// Inside your AppProvider function
const [showAuth, setShowAuth] = useState(false);       // Ensure FALSE
const [showFilters, setShowFilters] = useState(false); // Ensure FALSE
const [selectedHelpItem, setSelectedHelpItem] = useState(null); // Ensure NULL